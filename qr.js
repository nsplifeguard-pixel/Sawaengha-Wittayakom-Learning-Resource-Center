// QR Code module placeholder.
// In production connect a QR library such as QRCode.js or a server-side QR service.
// generateQR(text) should return a canvas/img representing the resource URL.
export function generateQR(text){return {value:text,ready:true}}
